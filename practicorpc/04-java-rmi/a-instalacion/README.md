# Java RMI — inciso a) instalación

Java RMI ya viene incluido dentro del **JDK**. No se instala por separado.

## Verificación realizada

En este entorno se verificó Java:

- `Java 21.0.10 LTS`

## Compilación manual típica

```bash
javac -d out src/ar/edu/ssdd/rmi/.../*.java
```
